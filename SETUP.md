# 타자 대회 (Firebase 버전)

학생들이 각자 다른 곳에서 접속해 여러 라운드를 겨루고, 라운드마다 순위 점수가 쌓여 종합 순위가 나옵니다.
선생님이 서버를 켤 필요가 없고, 주소가 고정됩니다.

준비에 20분쯤 걸립니다. 한 번만 하면 됩니다.

## 1. Firebase 프로젝트 만들기

1. [console.firebase.google.com](https://console.firebase.google.com) 접속
2. **프로젝트 만들기** → 이름 입력 (예: `typing-contest`)
3. Google 애널리틱스는 **사용 안 함**으로 두어도 됩니다

## 2. Firestore와 로그인 켜기

**빌드 → Firestore Database → 데이터베이스 만들기**
- 위치는 `asia-northeast3 (서울)`을 고르세요. 가까울수록 반응이 빠릅니다
- 규칙은 **프로덕션 모드**로 시작합니다. 어차피 4단계에서 바꿉니다

**빌드 → Authentication → 시작하기 → 익명**을 켭니다.
학생은 아이디를 만들 필요 없이 이름만 넣고 참가하게 됩니다.

## 3. 설정값 복사

프로젝트 개요 옆 **톱니바퀴 → 프로젝트 설정**으로 갑니다.
아래쪽 **내 앱**에서 웹 아이콘(`</>`)을 눌러 앱을 등록하면 이런 코드가 나옵니다.

```js
const firebaseConfig = {
  apiKey: "AIza...",
  authDomain: "typing-contest.firebaseapp.com",
  ...
};
```

이 값을 `firebase-config.js`에 그대로 옮겨 적으세요.

이 값들은 공개되어도 괜찮습니다. 실제 보안은 다음 단계의 규칙이 담당합니다.

## 4. 보안 규칙 배포 — 건너뛰지 마세요

**Firestore Database → 규칙** 탭을 열고, 기본 내용을 모두 지운 뒤 `firestore.rules` 내용을 붙여넣고 **게시**를 누릅니다.

이 규칙이 하는 일이 중요합니다.

- 학생이 제출한 문장이 **이번 라운드 원문과 정확히 같을 때만** 기록이 저장됩니다. 정확도 판정을 데이터베이스가 직접 하는 구조라, 브라우저에서 값을 조작해도 통과하지 못합니다
- 사람이 낼 수 없는 속도(글자당 25ms 미만)는 거부됩니다
- 학생은 자기 점수를 고칠 수 없습니다. 점수는 교사만 씁니다
- 한 번 낸 기록은 학생이 수정하거나 지울 수 없습니다

## 5. 웹에 올리기

정적 파일 네 개뿐이라 어디에 올려도 됩니다. GitHub Pages가 가장 간단합니다.

레포를 만들어 `index.html`, `host.html`, `hangul.js`, `firebase-init.js`, `firebase-config.js`를 올리고, **Settings → Pages**에서 브랜치를 지정하면 주소가 나옵니다.

**GitHub Pages를 쓸 때 한 가지 더 필요합니다.** Firebase 콘솔의 **Authentication → 설정 → 승인된 도메인**에 `사용자이름.github.io`를 추가하세요. 이걸 안 하면 로그인이 막혀 참가가 안 됩니다. Firebase Hosting으로 올리면 이 과정이 필요 없습니다.

## 대회 진행 방법

**선생님**은 `주소/host.html`로 들어가 [새 대회 열기]를 누릅니다. 6자리 대회 코드와 학생 접속 주소가 화면 위에 크게 뜹니다.

**학생**에게는 주소만 알려주면 됩니다. 주소에 코드가 들어 있어서 학생은 이름만 넣으면 참가됩니다. 코드를 따로 불러줄 필요가 없습니다.

참가자가 다 모이면 문장을 입력하고 [1라운드 시작]. 학생 화면에 카운트다운 후 문장이 뜨고, 타이머는 **각자 첫 글자를 친 순간** 시작합니다. 인터넷 속도가 느린 학생이 손해 보지 않도록 한 처리입니다.

왼쪽 [완주 현황]에서 누가 끝냈는지 이름 색으로 보입니다. 다 끝나면 [라운드 마감하고 점수 주기]를 누르세요. 1등 20점, 2등 19점 순으로 점수가 들어가고 최소 1점입니다. 완주하지 못하면 0점입니다.

다음 문장을 넣고 [2라운드 시작]으로 계속 이어가면 됩니다. [종합 순위] 탭에 누적 점수가 쌓입니다.

## 알아두면 좋은 것

**교사 화면은 만든 브라우저에서만 열립니다.** 대회를 만든 브라우저의 익명 계정이 주인으로 기록되기 때문입니다. 다른 PC에서 열거나 브라우저 데이터를 지우면 진행 권한을 잃습니다. 대회 당일에는 같은 PC, 같은 브라우저를 쓰세요. 실수로 창을 닫았다면 같은 브라우저에서 다시 열고 코드를 넣어 이어받을 수 있습니다.

**실시간 진행률은 보이지 않습니다.** 20명이 계속 진행률을 올리면 초당 수십 번 쓰기가 발생해 비용과 지연이 커집니다. 대신 완주 현황과 순위표가 실시간으로 채워집니다.

**붙여넣기는 막아뒀습니다.** 다만 원격 대회에서 화면 밖에서 무슨 일이 일어나는지는 알 수 없습니다. 비중이 큰 대회라면 화상회의로 화면을 함께 켜두는 편이 확실합니다.

**비용은 사실상 0원입니다.** 20명이 10라운드를 해도 쓰기 200회, 읽기 수천 회 수준이라 무료 한도 근처에도 가지 않습니다.

**인터넷이 끊기면 멈춥니다.** LAN 버전과 달리 외부 연결이 필수입니다. 대회 전에 접속 확인을 한 번 하시는 게 좋습니다.

## 파일

```
index.html          학생 화면 (참가 주소)
host.html           교사 화면 (대회 진행)
firebase-config.js  Firebase 설정값 — 3단계에서 채우는 파일
firebase-init.js    Firebase 초기화
hangul.js           자모 분해 · 타수 계산
firestore.rules     보안 규칙 — 4단계에서 콘솔에 붙여넣는 내용
```
